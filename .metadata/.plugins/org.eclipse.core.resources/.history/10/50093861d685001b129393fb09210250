/*$(function(){
	var term=$("#term").val();
	$("#submit").click(function(){
		console.log("dfs");
		
	  $.get("DictServlet", function(responseJson) {
			var parsedJsonValue=JSON.parse(responseJson);
	        var $ul = $("<ul>").appendTo($("#result"));
	        $.each(parsedJsonValue, function(index, item) {
	            $("<li>").text(item).appendTo($ul);
	        });

		});
    });
});*/

$(document).ready(function() {	
	$("#submit").click(function(e){		
    	e.preventDefault();
		var word = $("#term").val();
		
		$.ajax({
			"url": "DictServlet",
			"type": "GET",
			"data": { "word": word},
			"success": myAjaxSuccessFunction,
			"error": ajaxFailure
		});
		function myAjaxSuccessFunction(data) {
			// do something with the data
			//var $ul = $("<ol>").appendTo($("#result"));
	        $.each(data, function(index, item) {
	           // $("<li>").text(item.word).appendTo($ol);
					console.log(item);
					//console.log(item.definition);
	        });
		}
		function ajaxFailure(xhr, status, exception) {
			console.log(xhr, status, exception);
		}
		});
});